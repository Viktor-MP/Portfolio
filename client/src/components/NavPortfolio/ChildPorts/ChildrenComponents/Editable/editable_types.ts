import { ReactNode } from "react";

export type editablePropsType = {
    editableHeading: string;
    userName?: string;
    children?: ReactNode;
    className?: string;
    mode: boolean;
};

// export {}
